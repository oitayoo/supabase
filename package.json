{
	"name": "oitayoo-supabase",
	"version": "1.0.0",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"gen-types": "npx supabase gen types typescript --local > database.ts"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/oitayoo/supabase.git"
	}
}
